import { styled } from "@mui/material/styles";
import { StyledBoxChartWrapper } from "../../../../assets/styles/globalComponents.styles";

export const MonthlySalesChartWrapper = styled(StyledBoxChartWrapper)({
  marginTop: "1.5rem",
});
